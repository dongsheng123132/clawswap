# OpenClaw — Moltiverse 黑客松参赛方案

> Moltiverse Hackathon: $200K 奖金，2 周，AI Agent + Monad
> 核心评判: Agent 的创新性 + 实际能跑 + 社区参与

---

## 1. 一句话 Pitch

**"用户存 USDC 进来，AI Agent 7×24 小时帮你在 Monad 上交易赚钱。你睡觉，它盯盘。"**

---

## 2. 用户痛点（为什么人需要这个）

```
痛点 1: 人不能 24 小时盯盘
  → Agent 可以。每 10 秒检查一次价格，永不休息。

痛点 2: 人有情绪，追涨杀跌
  → Agent 严格按策略执行，不会 FOMO。

痛点 3: 链上操作太复杂
  → 用户只需要说"帮我定投 MON"，Agent 搞定一切。

痛点 4: 新币机会稍纵即逝
  → nad.fun 上新币出现到起飞可能只有几分钟，
     人来不及，Agent 毫秒级反应。
```

---

## 3. 赚钱闭环（Demo 必须展示的）

```
用户视角：

  存入 100 USDC
       │
       ▼
  选择策略: "每小时定投 10U 买 MON"
       │
       ▼
  授权 Agent (一键，10 秒完成)
       │
       ▼
  去睡觉 💤
       │
       ▼
  第二天起来看:
  ┌─────────────────────────────────┐
  │  Agent 已执行 24 笔交易          │
  │  花费: 96 USDC (4笔因波动跳过)   │
  │  买到: 240 MON                   │
  │  平均成本: 0.40 USDC/MON         │
  │  当前价格: 0.42 USDC/MON         │
  │  浮盈: +$4.80 (+5%)  📈         │
  └─────────────────────────────────┘

Agent 视角（后台自动执行的）：

  10:00 检查价格 0.41 → 买入 10U → 得到 24.3 MON ✅
  10:05 检查价格 0.41 → 未到时间，跳过
  ...
  11:00 检查价格 0.39 → 买入 10U → 得到 25.6 MON ✅ (便宜了多买点)
  11:30 检查价格 0.35 → 波动>10%，暂停保护 ⚠️
  12:00 检查价格 0.38 → 恢复正常，买入 10U ✅
  ...（24小时不停）
```

---

## 4. 黑客松 MVP — 只做这些

**砍掉 80% 的功能，只做能赢的核心。**

### 4.1 必须做的（MVP，参赛用）

| # | 功能 | 为什么必须 | 耗时 |
|---|------|-----------|------|
| 1 | Twitter 登录 → 自动建 AA 钱包 | 评委体验的第一步 | 1天 |
| 2 | 一键定投策略 | 核心卖点，最简单最能跑 | 2天 |
| 3 | Agent 自动执行 swap | 证明 Agent 真的在交易 | 2天 |
| 4 | 实时交易日志 | 评委要看到 Agent 在干活 | 1天 |
| 5 | 盈亏展示 | "看，Agent 帮我赚了 5%" | 0.5天 |
| 6 | x402 API（至少1个端点） | 展示 Agent 经济体的可能性 | 1天 |

### 4.2 加分项（有时间就做）

| # | 功能 | 加分点 |
|---|------|--------|
| 7 | 自然语言对话交易 | "帮我买100U的MON" → wow |
| 8 | 止盈止损策略 | 多一个策略选择 |
| 9 | nad.fun 新币监控 | 和 Moltiverse 主题强相关 |
| 10 | 嵌入百万格子 | 你的特色，差异化 |

### 4.3 不要做的（黑客松浪费时间）

- ❌ 自建 DEX 合约（直接用 Monad 上已有的 DEX）
- ❌ 完整前端 swap 界面（评委不看这个）
- ❌ 自建 x402 facilitator（用 Monad 官方的）
- ❌ 复杂的策略引擎（定投一个就够）
- ❌ 移动端适配（演示用电脑）

---

## 5. 简化后的架构

```
┌─────────────────────────────────────────┐
│            OpenClaw 网站                 │
│                                         │
│  ┌─────────────────────────────────┐    │
│  │  前端 (Next.js)                  │    │
│  │  • Twitter 登录 (Privy)         │    │
│  │  • 策略配置面板                  │    │
│  │  • 实时交易日志                  │    │
│  │  • 盈亏 Dashboard               │    │
│  └──────────────┬──────────────────┘    │
│                  │                       │
│  ┌───────────────▼──────────────────┐   │
│  │  后端 Agent (Node.js)            │   │
│  │  • 接收 Session Key              │   │
│  │  • 定时执行策略                   │   │
│  │  • 调用已有 DEX 的 Router swap    │   │
│  │  • x402 付费 API 端点            │   │
│  └───────────────┬──────────────────┘   │
│                   │                      │
│                   ▼                      │
│           Monad 链上                     │
│     已有 DEX (Kuru / Uniswap fork)      │
│     USDC / WMON 交易对                   │
└─────────────────────────────────────────┘
```

**关键简化：不自建 DEX 合约！** 直接调 Monad 上已有的 DEX Router。
黑客松评委看的是 Agent 的能力，不是你的 DEX 合约。

---

## 6. 黑客松 Demo 脚本（3 分钟）

### 第 1 分钟：展示问题 + 登录

```
"大家好，我是 OpenClaw。

 问题：你有没有因为没盯盘错过一个 10x 的机会？
 或者追涨买在最高点？

 AI Agent 不会。它 24 小时盯盘，严格按策略执行，不会 FOMO。

 [演示] 我现在用 Twitter 登录..."

→ 点 Twitter 登录
→ 自动创建 AA 钱包
→ 显示钱包地址 + 余额 (已提前充好测试币)
```

### 第 2 分钟：启动 Agent

```
"我设置一个定投策略：每分钟买 1 USDC 的 MON。"

→ 选择 DCA 策略
→ 配置参数（每分钟，1 USDC）
→ 点击启动 → Privy 确认授权
→ Agent 启动

"Agent 已经开始工作了。看，第一笔交易..."

→ 实时日志开始刷
→ ✅ 买入 1 USDC → 2.48 MON
→ 等 60 秒
→ ✅ 买入 1 USDC → 2.51 MON

"两笔了。Agent 会一直这样跑，24/7。
 我设置了安全限制：单笔最多 100U，每天最多 1000U，
 随时可以一键停止。"
```

### 第 3 分钟：x402 + 总结

```
"不只是给个人用。OpenClaw 的 API 通过 x402 协议对外开放。
 任何 AI Agent 付 $0.001 USDC 就能调用我的交易服务。"

→ 切到 Earn 页面 → 展示 API 调用统计

"这意味着 Agent 之间可以互相交易。
 一个 Agent 做分析，一个 Agent 做执行，通过 x402 自动结算。
 这就是 Agent 经济体。

 总结：
 1. 用户存钱进来 → Agent 自动交易 → 用户赚钱
 2. API 对外开放 → 其他 Agent 付费调用 → 平台赚钱
 3. 24/7 运行 → 人做不到，Agent 可以

 谢谢。"
```

---

## 7. 和百万格子的结合

你的百万格子项目可以这样集成 OpenClaw：

```
百万格子 (Million Pixel Grid)
┌─────┬─────┬─────┬─────┬─────┐
│     │     │     │     │     │
│ NFT │ Game│     │     │ ... │
│     │     │     │     │     │
├─────┼─────┼─────┼─────┼─────┤
│     │█████│     │     │     │
│     │█OC █│← OpenClaw 的格子  │
│     │█████│  点击进入交易界面  │
├─────┼─────┼─────┼─────┼─────┤
│     │     │     │     │     │
│ ... │ ... │ ... │ ... │ ... │
│     │     │     │     │     │
└─────┴─────┴─────┴─────┴─────┘

用户在百万格子上看到 OpenClaw 的格子
  → 点击 → 弹出 OpenClaw 迷你面板
  → 直接配置 Agent → 开始自动交易

格子本身也是广告位：
  "🤖 OpenClaw: 你的 AI 24h 帮你在 Monad 赚钱"
```

---

## 8. 参赛策略选择

### 选项 A: Agent+Token Track ($140K 奖池)

需要在 nad.fun 发一个 $CLAW 代币。

```
好处: 奖池大 ($100K + $40K 流动性)
做法:
  1. 在 nad.fun 发 $CLAW 代币
  2. OpenClaw Agent 自动做市 $CLAW（买卖维持价格）
  3. 用户用 $CLAW 当 OpenClaw 会员（享受低手续费）
  4. "Build in public" — 在 Twitter 上每天发 Agent 的交易记录
风险: 需要代币有市值，需要社区运营
```

### 选项 B: Agent Track ($60K 奖池)

不需要发代币，纯看 Agent 技术。

```
好处: 更纯粹，不用管代币运营
做法:
  1. 做好 Agent 的自动交易功能
  2. 做好 x402 API
  3. Demo 演示即可
风险: 奖池较小，竞争可能更卷技术
```

**建议: 选 Agent+Token Track。** 发代币不难（nad.fun SDK 几行代码），$140K 奖池更大，而且你的 Agent 可以自动做市 $CLAW，这本身就是一个很好的 demo。

---

## 9. 给 Cursor 的精简开发清单

```
=== Day 1-2: 基础框架 ===
1. Next.js 项目（前端 + API routes 在一个项目里，别分包了）
2. Privy 登录（Twitter + Email）
3. 链配置（Monad Testnet）
4. 首页：未登录→登录页，已登录→Dashboard

=== Day 3-4: Agent 核心 ===
5. 接入已有 DEX（找 Monad 上的 Uniswap fork 或 Kuru 的 Router 地址）
6. swap 功能（approve + exactInputSingle）
7. DCA 策略引擎（定时执行 swap）
8. Session Key 签发 + Agent 使用（如果来不及就先用简单的授权）

=== Day 5-6: 前端 Dashboard ===
9. 策略配置面板（选 DCA → 配参数 → 启动）
10. 实时交易日志（SSE 或轮询）
11. 余额 + 盈亏展示
12. 启动/停止 Agent 按钮

=== Day 7: x402 + 打磨 ===
13. x402 付费 API（至少 /api/quote 一个端点）
14. Earn 页面（展示 API 统计）
15. 测试网引导（水龙头链接）
16. 最终测试 + 录 Demo 视频
```

---

## 10. 关键技术决策（黑客松版本）

| 决策 | 之前方案 | 黑客松方案 | 理由 |
|------|---------|-----------|------|
| DEX | 自建 Uniswap V3 fork | 用已有 DEX | 省 3 天，评委不看合约 |
| 项目结构 | Monorepo 5 个包 | 一个 Next.js 全栈项目 | 简单快速 |
| AA 钱包 | ZeroDev Kernel + Session Key | Privy Embedded Wallet + 服务端代理 | 快速可用 |
| 策略 | DCA + 止损 + 狙击 + 网格 | 只做 DCA | 一个做好比四个半成品强 |
| Agent 运行 | 独立服务 | API route + cron | 简单 |
